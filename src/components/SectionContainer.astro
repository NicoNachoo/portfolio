---
const { title, id, class: className = "" } = Astro.props;
---

<section
    id={id}
    class={`h-full lg:w-1/3 xl:pt-0 py-4 xl:mt-0 mt-10 flex justify-end flex-col ${className}`}
>
    <h2 class="xl:sticky xl:top-0 xl:z-20 xl:pt-4 text-4xl mb-8 flex justify-center font-extrabold">
        {title}
    </h2>
    <slot />
</section>
